backend: omlx

# omlx: block intentionally absent — omlx uses built-in defaults.
# Set max_process_memory, paged_ssd_cache_dir, etc. here when tuning.

models:
  - model_path: mlx-community/Qwen3-Coder-Next-4bit
    served_model_name: qwen3-coder-next
    model_type: lm
    pin: true
    ttl: null

  - model_path: mlx-community/bge-m3
    served_model_name: bge-m3
    model_type: lm
    ttl: 600
