# omlx is not on PyPI; installed from git — see install.sh step 9b.
# Pinned by commit SHA (OMLX_GIT_REF) in install.sh; bump deliberately.
# omlx @ git+https://github.com/jundot/omlx.git@<sha>
open-webui==0.9.2
huggingface_hub[cli]==1.14.0
